<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>InfoNova</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: white;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      min-height: 100vh;
    }
    header {
      text-align: center;
      margin-top: 20px;
    }
    #logo {
      max-width: 150px;
    }
    #search-bar {
      margin: 20px;
      width: 80%;
      max-width: 600px;
    }
    #question-input {
      width: 100%;
      padding: 15px;
      font-size: 18px;
      border-radius: 25px;
      border: 1px solid #ccc;
      box-shadow: 1px 1px 5px rgba(0,0,0,0.1);
    }
    #result {
      margin-top: 20px;
      padding: 10px;
      max-width: 600px;
      text-align: center;
      font-size: 18px;
    }
    footer {
      position: fixed;
      bottom: 10px;
      font-size: 16px;
      color: #444;
    }
  </style>
</head>
<body>
  <header>
    <img id="logo" src="robot_logo.jpg" alt="InfoNova logo">
    <h1>InfoNova</h1>
  </header>
  
  <div id="search-bar">
    <input id="question-input" type="text" placeholder="Quel genre de vidéo vous plaît ?">
  </div>
  
  <div id="result">Posez-moi n'importe quelle question</div>
  
  <footer>
    🤖 InfoNova – Petit robot toujours prêt à répondre
  </footer>

  <script src="data.js"></script>
  <script>
    const input = document.getElementById("question-input");
    const result = document.getElementById("result");

    input.addEventListener("keydown", function(event) {
      if (event.key === "Enter") {
        const question = input.value.trim().toLowerCase();
        if (questions[question]) {
          result.textContent = questions[question];
        } else if (gentils.includes(question)) {
          result.textContent = "Merci pour ton mot gentil ! 😊";
        } else {
          result.textContent = "Désolé, je n'ai pas encore la réponse à cette question.";
        }
      }
    });
  </script>
</body>
</html>
